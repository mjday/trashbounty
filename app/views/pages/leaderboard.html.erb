<div class="container">
  <div class="row justify-content-center align-items-center">
    <div class="col-xs-12 col-md-12 col-12">

      <h1 class="text-center">Top Leaderboard</h1>

      <p class="text-center"><strong><%= @sum %></strong> KG collected so far </p>

      <table id="dtDynamicVerticalScrollExample" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
        <thead>
          <tr>
            <th class="th-sm">Rank</th>
<!--             <th class="th-sm">User ID</th>
 -->            <th class="th-sm">Username</th>
            <th class="th-sm">Total Kg</th>
          </tr>
        </thead>
        <tbody>
          <% counter = 0 %>
          <% @users.each do |user| %>
            <%# user.collections.each do |collection| %>
          <tr>

            <td><%= counter += 1 %></td>
<!--             <td><%= user.id %></td>
 -->            <td>@<%= user.name %></td>

            <!-- shouldnt be taking the Collections total kg -->
            <td><%= user.tot_kg %></td>


            <%# end %> <!-- end users.collections.each -->
          </tr>
          <% end %> <!-- end users.each -->
        </tbody>
      </table>
     </div>
  </div>
</div>

